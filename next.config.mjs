/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','assets.aceternity.com','via.placeholder.com','source.unsplash.com'],
    },
};

export default nextConfig;
